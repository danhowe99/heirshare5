// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.howed.heirshare5.admin.web;

import com.howed.heirshare5.admin.web.AdminInventoryItemController;
import com.howed.heirshare5.domain.Estate;
import com.howed.heirshare5.domain.InventoryItem;
import com.howed.heirshare5.service.EstateService;
import com.howed.heirshare5.service.InventoryItemService;
import java.io.UnsupportedEncodingException;
import java.util.ArrayList;
import java.util.List;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RequestParam;
import org.springframework.web.util.UriUtils;
import org.springframework.web.util.WebUtils;

privileged aspect AdminInventoryItemController_Roo_Controller {
    
    @Autowired
    InventoryItemService AdminInventoryItemController.inventoryItemService;
    
    @Autowired
    EstateService AdminInventoryItemController.estateService;
    
    @RequestMapping(method = RequestMethod.POST, produces = "text/html")
    public String AdminInventoryItemController.create(@Valid InventoryItem inventoryItem, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, inventoryItem);
            return "admin/inventoryItem/create";
        }
        uiModel.asMap().clear();
        inventoryItemService.saveInventoryItem(inventoryItem);
        return "redirect:/admin/inventoryItem/" + encodeUrlPathSegment(inventoryItem.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(params = "form", produces = "text/html")
    public String AdminInventoryItemController.createForm(Model uiModel) {
        populateEditForm(uiModel, new InventoryItem());
        List<String[]> dependencies = new ArrayList<String[]>();
        if (estateService.countAllEstates() == 0) {
            dependencies.add(new String[] { "estate", "admin/estate" });
        }
        uiModel.addAttribute("dependencies", dependencies);
        return "admin/inventoryItem/create";
    }
    
    @RequestMapping(value = "/{id}", produces = "text/html")
    public String AdminInventoryItemController.show(@PathVariable("id") Long id, Model uiModel) {
        uiModel.addAttribute("inventoryitem", inventoryItemService.findInventoryItem(id));
        uiModel.addAttribute("itemId", id);
        return "admin/inventoryItem/show";
    }
    
    @RequestMapping(produces = "text/html")
    public String AdminInventoryItemController.list(@RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        if (page != null || size != null) {
            int sizeNo = size == null ? 10 : size.intValue();
            final int firstResult = page == null ? 0 : (page.intValue() - 1) * sizeNo;
            uiModel.addAttribute("inventoryitems", inventoryItemService.findInventoryItemEntries(firstResult, sizeNo));
            float nrOfPages = (float) inventoryItemService.countAllInventoryItems() / sizeNo;
            uiModel.addAttribute("maxPages", (int) ((nrOfPages > (int) nrOfPages || nrOfPages == 0.0) ? nrOfPages + 1 : nrOfPages));
        } else {
            uiModel.addAttribute("inventoryitems", inventoryItemService.findAllInventoryItems());
        }
        return "admin/inventoryItem/list";
    }
    
    @RequestMapping(method = RequestMethod.PUT, produces = "text/html")
    public String AdminInventoryItemController.update(@Valid InventoryItem inventoryItem, BindingResult bindingResult, Model uiModel, HttpServletRequest httpServletRequest) {
        if (bindingResult.hasErrors()) {
            populateEditForm(uiModel, inventoryItem);
            return "admin/inventoryItem/update";
        }
        uiModel.asMap().clear();
        inventoryItemService.updateInventoryItem(inventoryItem);
        return "redirect:/admin/inventoryItem/" + encodeUrlPathSegment(inventoryItem.getId().toString(), httpServletRequest);
    }
    
    @RequestMapping(value = "/{id}", params = "form", produces = "text/html")
    public String AdminInventoryItemController.updateForm(@PathVariable("id") Long id, Model uiModel) {
        populateEditForm(uiModel, inventoryItemService.findInventoryItem(id));
        return "admin/inventoryItem/update";
    }
    
    @RequestMapping(value = "/{id}", method = RequestMethod.DELETE, produces = "text/html")
    public String AdminInventoryItemController.delete(@PathVariable("id") Long id, @RequestParam(value = "page", required = false) Integer page, @RequestParam(value = "size", required = false) Integer size, Model uiModel) {
        InventoryItem inventoryItem = inventoryItemService.findInventoryItem(id);
        inventoryItemService.deleteInventoryItem(inventoryItem);
        uiModel.asMap().clear();
        uiModel.addAttribute("page", (page == null) ? "1" : page.toString());
        uiModel.addAttribute("size", (size == null) ? "10" : size.toString());
        return "redirect:/admin/inventoryItem";
    }
    
    void AdminInventoryItemController.populateEditForm(Model uiModel, InventoryItem inventoryItem) {
        uiModel.addAttribute("inventoryItem", inventoryItem);
        uiModel.addAttribute("estates", estateService.findAllEstates());
    }
    
    String AdminInventoryItemController.encodeUrlPathSegment(String pathSegment, HttpServletRequest httpServletRequest) {
        String enc = httpServletRequest.getCharacterEncoding();
        if (enc == null) {
            enc = WebUtils.DEFAULT_CHARACTER_ENCODING;
        }
        try {
            pathSegment = UriUtils.encodePathSegment(pathSegment, enc);
        } catch (UnsupportedEncodingException uee) {}
        return pathSegment;
    }
    
}
